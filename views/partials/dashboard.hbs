<div class="container mt-4">
    <h1 class="text-center">Nuestros Productos</h1>
    <div class="row mb-5">
        {{#each productos}}
        {{> producto this}}
        {{/each}}
    </div>
</div>